name: "HA-Tools Tunnel"
version: "1.0.12"
slug: "frpc_tunnel"
description: "Reverse-Tunnel zum HA-Tools Server"
startup: services
boot: auto
init: false
ingress: false

options:
  SERVER_ADDR: "ha-cloud.example.com"
  SERVER_PORT: 7000
  SUBDOMAIN: "kunde1"

schema:
  SERVER_ADDR: str
  SERVER_PORT: int
  SUBDOMAIN: str

environment:
  SERVER_ADDR: $SERVER_ADDR
  SERVER_PORT: $SERVER_PORT
  SUBDOMAIN: $SUBDOMAIN

arch:
  - aarch64
  - amd64
  - armhf
  - armv7
  - i386
